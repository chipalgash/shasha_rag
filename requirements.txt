# requirements.txt

# Core
python-docx>=1.1.0
razdel>=0.5.0
rank-bm25>=0.2.2
numpy>=1.24.0
faiss-cpu>=1.7.4
sentence-transformers>=3.0.0
transformers>=4.41.0
# Install torch per your platform/GPU if this line fails; otherwise use:
# pip3 install torch --index-url https://download.pytorch.org/whl/cu121
torch>=2.2.0

# Backends
requests>=2.31.0 # Needed for Ollama backend HTTP

# Optional (Colab / low VRAM)
bitsandbytes>=0.43.0
accelerate>=0.30.0